# Core ML (one of these required)
torch>=2.0
onnxruntime>=1.16
onnx>=1.15
numpy>=1.24

# AMD GPU support (optional — install ONE of these)
# For AMD ROCm GPUs on Linux: pip install torch==2.5.1+rocm6.2 --index-url https://download.pytorch.org/whl/rocm6.2
# For AMD/Intel GPUs on Windows: pip install onnxruntime-directml
# For AMD/Intel GPUs on Linux: pip install onnxruntime-directml (experimental)

# Astronomical data
astropy>=5.0
lightkurve>=2.4
scipy>=1.10

# Model weights
huggingface_hub>=0.14
safetensors>=0.4

# IPFS
ipfshttpclient>=0.8

# Image processing
Pillow>=9.0

# HTTP requests
requests>=2.28

# Mining pool (Stratum protocol)
websockets>=11.0

# Scikit-learn (for anomaly detection)
scikit-learn>=1.2
